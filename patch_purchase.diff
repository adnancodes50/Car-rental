*** Begin Patch
*** Update File: resources\views\models\purchase.blade.php
@@
-                        <div class="col-12">
-                            <label class="form-label">Email</label>
-                            <input type="email" name="email" class="form-control rounded-3" placeholder="you@example.com" required>
-                        </div>
-                        <div class="col-12">
-                            <label class="form-label">Phone</label>
-                            <input type="tel" name="phone" class="form-control rounded-3" placeholder="+27 123 456 7890" required>
-                        </div>
+                        <div class="col-12">
+                            <label class="form-label">Email</label>
+                            <input type="email" name="email" class="form-control rounded-3" placeholder="you@example.com"
+                                inputmode="email" autocomplete="email" required
+                                pattern="^([^\s@]+)@([^\s@]+)\.[^\s@]{2,}$"
+                                title="Enter a valid email address, e.g. you@example.com">
+                        </div>
+                        <div class="col-12">
+                            <label class="form-label">Phone</label>
+                            <input type="tel" name="phone" class="form-control rounded-3" placeholder="+27 123 456 7890"
+                                inputmode="tel" required
+                                pattern="^\+?[0-9]{1,4}(?:[\s-]?[0-9]{2,4}){2,4}$"
+                                title="Use digits, optional spaces or dashes, e.g. +27 123 456 7890">
+                        </div>
@@
-    $('backToStep1').addEventListener('click', () => swapModal('purchaseCustomer','purchaseModal'));
+    $('backToStep1').addEventListener('click', () => swapModal('purchaseCustomer','purchaseModal'));
 
     // Step 2 ? Payment (save details first)
     $('purchaseStep2Next').addEventListener('click', async () => {
-      const name    = (form.name?.value || '').trim();
-      const email   = (form.email?.value || '').trim();
-      const phone   = (form.phone?.value || '').trim();
-      const country = form.country?.value;
-
-      if (!name || !email || !phone || !country) {
-        Swal.fire({ icon:'error', title:'Missing Information', text:'Please fill in all required customer details.' });
-        return;
-      }
+      const nameField  = form.name;
+      const emailField = form.email;
+      const phoneField = form.phone;
+      const country    = form.country;
+
+      const name       = (nameField?.value || '').trim();
+      const email      = (emailField?.value || '').trim();
+      const phone      = (phoneField?.value || '').trim();
+      const countryVal = country?.value || '';
+
+      if (emailField) emailField.value = email;
+      if (phoneField) phoneField.value = phone;
+
+      const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]{2,}$/;
+      const phonePattern = /^\+?[0-9]{1,4}(?:[\s-]?[0-9]{2,4}){2,4}$/;
+
+      if (!name || !email || !phone || !countryVal) {
+        Swal.fire({ icon:'error', title:'Missing Information', text:'Please fill in all required customer details.' });
+        return;
+      }
+
+      if (!emailPattern.test(email)) {
+        Swal.fire({ icon:'error', title:'Invalid Email', text:'Enter a valid email address, e.g. you@example.com.' });
+        emailField?.focus();
+        return;
+      }
+
+      if (!phonePattern.test(phone)) {
+        Swal.fire({ icon:'error', title:'Invalid Phone Number', text:'Use digits with optional spaces or dashes, e.g. +27 123 456 7890.' });
+        phoneField?.focus();
+        return;
+      }
 
       try {
         const res = await fetch("{{ route('purchase.store') }}", {
           method:'POST',
           headers:{ 'Content-Type':'application/json', 'X-CSRF-TOKEN':"{{ csrf_token() }}" },
-          body: JSON.stringify({ name, email, phone, country, vehicle_id: form.vehicle_id.value, total_price: form.total_price.value })
+          body: JSON.stringify({ name, email, phone, country: countryVal, vehicle_id: form.vehicle_id.value, total_price: form.total_price.value })
         });
*** End Patch
